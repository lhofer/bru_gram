<%= link_to image_tag @photo.url %>
<%= @photo.caption %>

<h1>Comments</h1>
<%= render @photo.comments %>

<h1>Add A Comment</h1>
<%= render 'comments/form' %>

<%= link_to 'Back', user_photos_path(current_user) %>
