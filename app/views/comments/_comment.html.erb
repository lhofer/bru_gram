<h3>Comment: <%= comment.commenter %></h3>
<h3>Commenter: <%= comment.body %></h3>

<p>
	<%= link_to 'Edit Comment', edit_photo_comment_path(comment.photo, comment) %>
	<%= link_to 'Delete Comment', [comment.photo, comment],
		method: :delete,
		data: { confirm: 'Are you sure?'} %>
</p>